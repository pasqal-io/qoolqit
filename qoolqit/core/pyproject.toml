[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qoolqit-core"
version = "1.1.0"
requires-python = ">=3.10"

dependencies = [
  "pulser_core>=1.8.0",
]

[tool.hatch.build.targets.wheel]
packages = ["qoolqit"]