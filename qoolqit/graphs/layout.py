from __future__ import annotations

from .graph import BaseGraph


class Layout(BaseGraph):
    """A graph representing a layout of points.

    This can be used, for example, as a target for embedding on specific patterns.
    """

    pass
